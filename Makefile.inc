#

COPTS+=		-isystem ${BSDSRCDIR}/include -DOBASE -Wall
COPTS+=		-DMACHINE='"${MACHINE}"' -DMACHINE_ARCH='"${MACHINE_ARCH}"'
COPTS+=		-D_GNU_SOURCE
HOSTCC=		${CC}
HOSTCFLAGS+=	${COPTS}
LIBOBASE=	${BSDSRCDIR}/lib/libobase/libobase.a
LIBDB=		${BSDSRCDIR}/lib/libc/db/libdb.a
DPADD+=		${LIBOBASE}
LDADD+=		${LIBOBASE}
